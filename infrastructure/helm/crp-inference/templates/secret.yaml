{{- if .Values.redis.authToken }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.redis.existingSecret }}
  namespace: {{ .Values.namespace.name }}
type: Opaque
stringData:
  {{ .Values.redis.authTokenKey }}: {{ .Values.redis.authToken | quote }}
{{- end }}
